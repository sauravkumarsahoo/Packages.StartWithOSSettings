<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<Authors>Saurav Kumar Sahoo (clicksrv)</Authors>
		<TargetFramework>net6.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<Version>0.1.0</Version>
		<Company>clicksrv</Company>
		<Description>A package that helps in configuring Startup Settings for any program. Also supports configuring enabling and disabling of the entry apart from creation of Startup Entry.
Currently supports Windows only with plans to eventually expand to other OSes.</Description>
		<PackageProjectUrl>https://github.com/clicksrv/StartWithOSSettings</PackageProjectUrl>
		<PackageReadmeFile>README.md</PackageReadmeFile>
		<RepositoryUrl>https://github.com/clicksrv/StartWithOSSettings</RepositoryUrl>
		<RepositoryType>git</RepositoryType>
		<PackageTags>startup;os;boot;start-up;windows;manage startup;configure startup;start with windows;start on boot;start with os</PackageTags>
		<PackageLicenseExpression>https://opensource.org/licenses/Apache-2.0</PackageLicenseExpression>
		
	</PropertyGroup>

	<ItemGroup>
	  <None Update="README.md">
	    <Pack>True</Pack>
	    <PackagePath>\</PackagePath>
	  </None>
	</ItemGroup>

</Project>
